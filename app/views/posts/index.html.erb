<div class="flex justify-center items-center py-12 px-4">
  <div class="min-h-screen">
    <h1 class="mb-14 text-center text-3xl font-bold tracking-tight text-gray-900"><%= t '.title' %></h1>
      <%= render @posts %>  
      <%= paginate @posts %>
  </div>
</div>